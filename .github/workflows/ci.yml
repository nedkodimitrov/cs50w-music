name: Testing
on: push

jobs:
  test_project:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v4
    - name: Run Django unit tests
      run: |
        pip3 install --user django
        pip3 install --user djangorestframework
        python3 cs50w_music/manage.py test